let initState = {};

function Reducer(state = initState, action) {
  const { type, payload } = action;
  switch (type) {
    // case "":
    //   return { ...state };
    default:
      return { ...state };
  }
}

export default Reducer;
